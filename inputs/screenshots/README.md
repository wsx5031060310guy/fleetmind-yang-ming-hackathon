# Screenshots

This folder stores source screenshots that are useful for team reference.

Current approach:

- Store cleaner slide-only screenshots when available.
- Avoid sharing Teams UI screenshots with participant panels unless necessary.
- Convert screenshots into Markdown notes under `docs/` or `meetings/` so AI agents can read the content without image OCR.


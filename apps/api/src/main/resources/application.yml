spring:
  application:
    name: fleetmind-api
  servlet:
    multipart:
      max-file-size: 12MB
      max-request-size: 12MB

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: ${PORT:8080}

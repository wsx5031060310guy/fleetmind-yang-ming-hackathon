# Samples

Synthetic data only. Do not place Yang Ming enterprise data in this folder.

Use these files to smoke-test local scripts before Day1.

- `noon-reports.csv` is tiny synthetic input for `demo-local` and export smoke tests.
- `schema-map.template.csv` is a safe template for Day1 field mapping; copy it to an ignored working path before filling real field names.
